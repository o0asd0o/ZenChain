<!-- zenchain:agent-skills:start -->

## Agent skills

Generated by `zen render` from `.zenchain/config.env`. Edit that file and re-render
rather than editing between the markers — a re-render replaces this block.

### Issue tracker

{{TRACKER_ONE_LINER}} See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles, recorded as {{STATUS_MECH}}. See `docs/agents/triage-labels.md`.

### Planning modifiers

- When running `/grill-with-docs`, read `docs/agents/grill-with-docs-policy.md`; it limits documentation without modifying the upstream skill.
- When running `/to-tickets` or `/to-issues`, read `docs/agents/ticket-writing-policy.md`; it defines the tracker-neutral agent-ready ticket packet.

### Human decision queue

Open questions live only in `docs/INBOX.md`. The issue packet, not the queue entry, is the implementation contract after the human answers.

### The pipeline

`{{PIPELINE_DIR}}/ORCHESTRATOR.md` drives issues to merged, unattended, using six roles. It starts where the planning skills stop — see `docs/agents/flow.md` for the seam and the on-ramps that feed it.

### Code standards

Scope discipline, one component per file, helper placement, the routes/features split, and commenting. See `docs/agents/code-standards.md` — read by the roles that write or judge code, not loaded here.

<!-- zenchain:agent-skills:end -->
